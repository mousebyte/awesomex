return {
    focus_indicator = require("util.widget.focus-indicator"),
    action_box      = require("util.widget.action-box"),
    fuzzy_select    = require("util.widget.fuzzy-select"),
    bind            = require("util.moon.bind"),
    screen_handler  = require("util.screen-handler"),
    animate         = require("util.animate"),
    fzy             = require("fzy_lua")
}
